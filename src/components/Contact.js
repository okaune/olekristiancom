import React from 'react'

import '../styles/css/Contact.css'

const Contact = props => (
    <section className="contact">
        <h2 className="contact__header">Noe du lurer på?</h2>
        <a href="mailto:post@olekristian.com" className="contact__button">Kontakt meg</a>
    </section>
)

export default Contact